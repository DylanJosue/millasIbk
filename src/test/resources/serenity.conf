#
# This section defines environment-specific configuration for different environments.
#

environments {
  default {
    url = "https://apis.uat.interbank.pe"
  }
  dev {
    url = "https://apis.dev.interbank.pe"
  }
  uat {
    url = "https://apis.uat.interbank.pe"
  }
}